# Clean implementation

`wound_forecasting.metrics` contains the corrected common evaluation used for
the final paper tables. `wound_forecasting.manifests` contains explicit
subject-level split helpers. Do not import production code from the frozen
notebooks.
